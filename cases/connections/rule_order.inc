include:
  - path: styles.inc

diagram:
  label: Rule Application Order

blocks:

  - name: X
    style: title_block
    label: |-
      Rules must be applied in an appropriate order
      to avoid intersections.
